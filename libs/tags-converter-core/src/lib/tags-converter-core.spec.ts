import { tagsConverterCore } from './tags-converter-core';

describe('tagsConverterCore', () => {
  it('should work', () => {
    expect(tagsConverterCore()).toEqual('tags-converter-core');
  });
});
